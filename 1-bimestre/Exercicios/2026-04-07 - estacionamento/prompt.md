Aja como um Desenvolvedor Front-end Sênior e Professor. Eu tenho um código funcional (ou uma ideia de lógica) para um pequeno sistema e preciso que você o refatore e estilize aplicando as melhores práticas de mercado.

**Contexto do Sistema:**
[Explique brevemente o que o sistema faz. Ex: Uma calculadora de IMC, um sistema de notas escolares, um conversor de moedas, etc.]

**Regras de Negócio / Requisitos:**
[Liste as regras matemáticas ou lógicas que o sistema deve seguir. Ex: A nota média é 7. Se for menor, está reprovado.]

**Meu Código Atual (se houver):**
[Cole aqui o seu código HTML/JS inicial ou diga "Crie do zero baseado nas regras acima"]

**Requisitos Técnicos Obrigatórios (Mantenha este padrão):**

* **Arquitetura:** Divida a solução em três arquivos separados (`index.html`, `style.css` e `script.js`).
* **HTML:** Use tags semânticas (`<main>`, `<header>`, `<section>`, `<form>`) e adapte entradas de "Sim/Não" para caixas de seleção (`checkbox`), se aplicável.
* **CSS (Variáveis):** Crie variáveis no `:root` utilizando a seguinte paleta: fundo predominante branco/cinza muito claro, textos e títulos em tons de Azul Escuro, e botões/destaques em Aquamarine.
* **CSS (Layout):** O container principal deve ocupar 80% da tela na horizontal, centralizado e ajustado para caber perfeitamente em uma tela de 1080p na vertical sem sobras exageradas. O design deve ser moderno, limpo e profissional.
* **UX/Avisos:** Crie um container específico para exibir alertas ou erros de validação ao usuário (ex: entradas vazias ou negativas). Este container deve ter fundo vermelho, texto branco e ficar oculto por padrão.
* **JavaScript:** Inclua comentários didáticos explicando o passo a passo da lógica. Adicione validações de segurança básicas para impedir que o sistema quebre com entradas inválidas (como letras onde deveriam ser números, ou valores negativos). Exiba o resultado final com formatação adequada (ex: duas casas decimais para moeda).